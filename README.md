## Installation

1. Install UV (include link to uv curl command)
2. Install python dependencies: `uv sync`
3. Install GraphViz (required for diagrams): `sudo apt install graphviz`
4. Install xdg-utils: `sudo apt install xdg-utils`